<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.greenshadow.costbook">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_cost_icon_2"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_cost_icon_2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".avtivity.IntoActivity"
            android:theme="@style/AppTheme.Into">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".avtivity.HomeActivity" />

        <activity android:name=".avtivity.AddCostActivity" />

        <activity android:name=".avtivity.ThreadListActivity" />

        <provider
            android:name=".provider.CostListProvider"
            android:authorities="com.greenshadow.cost.list"
            android:exported="false" />
    </application>

</manifest>